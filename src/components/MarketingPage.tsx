import Link from "next/link"; import { PageShell } from "@/components/PageShell"; type LinkItem = { href: string; label: string; tone?: "primary" | "secondary"; }; type Feature = { title: string; description: string; href?: string; }; export function MarketingPage({ title, description, intro, links, stats, features, footnote, }: { title: string; description: string; intro: string; links: LinkItem[]; stats?: Array<{ value: string; label: string }>; features: Feature[]; footnote?: string; }) { return ( <PageShell title={title} description={description}> <div className="space-y-10"> <section className=" border border-slate-200 bg-white p-8 shadow-sm"> <p className="text-sm font-medium uppercase tracking-[0.2em] text-slate-500"> Premium consultancy platform </p> <h3 className="mt-4 text-3xl font-semibold tracking-tight text-slate-900"> {title} </h3> <p className="mt-4 max-w-3xl text-lg leading-8 text-slate-600"> {intro} </p> <div className="mt-8 flex flex-wrap gap-3"> {links.map((link) => ( <Link key={link.href} href={link.href} className={` px-5 py-3 text-sm font-medium transition ${ link.tone === "primary" ? "bg-slate-900 text-white hover:bg-slate-700" : "border border-slate-300 text-slate-700 hover:bg-slate-100" }`} > {link.label} </Link> ))} </div> </section> {stats && stats.length > 0 ? ( <div className="grid gap-4 md:grid-cols-3"> {stats.map((stat) => ( <div key={stat.label} className=" border border-slate-200 bg-white p-6 shadow-sm" > <p className="text-3xl font-semibold text-slate-900"> {stat.value} </p> <p className="mt-2 text-sm text-slate-600">{stat.label}</p> </div> ))} </div> ) : null} <div className="grid gap-6 lg:grid-cols-2"> {features.map((feature) => ( <div key={feature.title} className=" border border-slate-200 bg-white p-7 shadow-sm" > <h4 className="text-xl font-semibold text-slate-900"> {feature.title} </h4> <p className="mt-3 text-sm leading-7 text-slate-600"> {feature.description} </p> {feature.href ? ( <Link href={feature.href} className="mt-5 inline-flex text-sm font-semibold text-slate-900 underline decoration-slate-300 underline-offset-4" > Explore more </Link> ) : null} </div> ))} </div> {footnote ? ( <p className=" border border-dashed border-slate-300 bg-slate-100 p-6 text-sm text-slate-600"> {footnote} </p> ) : null} </div> </PageShell> ); } 